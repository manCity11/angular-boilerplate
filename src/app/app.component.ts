import { Component } from "@angular/core";
const template = require("./app.html"); // crappy but because of TypeScript

@Component({
  selector: "app-root",
  template
})
export class AppComponent {}
